Drop your wedding nasheed MP3 here, named exactly:

    wedding-nasheed.mp3

so the final path is:  public/audio/wedding-nasheed.mp3

The site code already points to this exact path (BackgroundMusic.jsx), so once
you add the file, everything works with zero code changes. To use a different
track later, just replace this file with another MP3 of the same name — no
rebuild or code edit required, since Vite serves everything in /public as-is.

Note: I can't source or embed a copyrighted nasheed track for you — you'll
need to add a track you have the rights to use (purchased, licensed, or
royalty-free).
